# System Role

Installs base system packages and configures power management.

## What it does

- Installs essential system utilities (curl, wget, git, build-essential, etc.)
- Installs and configures TLP for laptop power management
- Updates apt cache
