# Audio Role

Installs and configures PipeWire audio stack.

## What it does

- Installs PipeWire (modern audio/video server)
- Installs PulseAudio compatibility layer
- Installs WirePlumber (PipeWire session manager)
- Installs audio utilities (pavucontrol, pulsemixer, playerctl)
